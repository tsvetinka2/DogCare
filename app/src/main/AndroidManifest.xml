<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dogcare">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DogCare">
        <activity android:name=".SetNewPassword"></activity>
        <activity android:name=".VerifyCode" />
        <activity android:name=".ForgetPassSuccessMessage" />
        <activity android:name=".MakeSelection" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".DogActivityNew" />
        <activity android:name=".AddDog" />
        <activity android:name=".UserProfile" />
        <activity android:name=".FirstAct" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>